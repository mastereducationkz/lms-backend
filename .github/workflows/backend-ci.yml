name: Backend CI

on:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "alembic/**"
      - "tests/**"
      - "scripts/**"
      - "requirements.txt"
      - "Dockerfile"
      - "docker-compose.yml"
      - "alembic.ini"
      - ".github/workflows/backend-ci.yml"
      - ".github/workflows/deploy-backend.yml"
  push:
    branches: [main]
    paths:
      - "src/**"
      - "alembic/**"
      - "tests/**"
      - "scripts/**"
      - "requirements.txt"
      - "Dockerfile"
      - "docker-compose.yml"
      - "alembic.ini"
      - ".github/workflows/backend-ci.yml"
      - ".github/workflows/deploy-backend.yml"

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies
        run: pip install -r requirements.txt

      - name: Run backend tests
        run: |
          set +e
          pytest -q
          code=$?
          set -e
          if [ "$code" -eq 5 ]; then
            echo "No tests collected, skipping test failure."
            exit 0
          fi
          exit "$code"

      - name: Security - pip-audit (dependency vulnerabilities)
        run: pip install pip-audit -q && pip-audit --desc || true

      - name: Security - bandit (SAST)
        run: pip install bandit -q && bandit -r src -ll -q || true

      - name: Build backend Docker image
        run: docker build -t lms-backend-ci .
